$(document).ready(function() {
  $("form#questions").submit(function() {
    event.preventDefault();
    let count = 0;
    if (question1, question2, question3 === "yes") {
    count++;
    }
      if (count === "3") {
      alert("CAT!")
      }

  });
});
